export const baseDomain = "https://ismart-rising-star.herokuapp.com/";
export const baseURL = baseDomain + "api/";

const API = {
  getBusinessClassListAPI: baseURL + "businesses/",
};

export default API;
